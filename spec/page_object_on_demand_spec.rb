require 'spec_helper'

describe PageObjectOnDemand do
  it 'has a version number' do
    expect(PageObjectOnDemand::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
